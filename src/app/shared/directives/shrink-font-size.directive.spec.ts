import { ShrinkFontSizeDirective } from './shrink-font-size.directive';

describe('ShrinkFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShrinkFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
